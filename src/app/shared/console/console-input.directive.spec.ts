import { ConsoleInputDirective } from './console-input.directive';

describe('ConsoleInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ConsoleInputDirective();
    expect(directive).toBeTruthy();
  });
});
